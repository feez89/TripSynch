# ── Database (already on Supabase) ───────────────────────────────────────────
DATABASE_URL="postgresql://postgres:PASSWORD@db.cbktzkiyzqbcmibmfgbr.supabase.co:5432/postgres"

# ── Auth (use the generated values below) ────────────────────────────────────
JWT_SECRET="5f86c977262f1b691b98df1d2a5a994313230fa224af283994e73c60d46d1320"
JWT_REFRESH_SECRET="f3bd13fd45bf377f74fd0927c581b0dfb0b393acbfb9c1d939f8d80cb3e8d7af"
JWT_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN="30d"

# ── Redis (optional — use Upstash free tier) ──────────────────────────────────
# Leave blank to run without caching (flight/stay results won't be cached)
REDIS_URL=""

# ── Server ───────────────────────────────────────────────────────────────────
PORT=3001
NODE_ENV="production"

# ── CORS (set to your Vercel frontend URL once deployed) ─────────────────────
CORS_ORIGIN="https://tripsync.vercel.app"

# ── AI & Providers (mock by default, swap for real APIs later) ───────────────
AI_PROVIDER="mock"
FLIGHT_PROVIDER="mock"
STAY_PROVIDER="mock"
OPENAI_API_KEY=""
